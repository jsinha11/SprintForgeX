# Contributing

- Create a feature or chore branch from `main`.
- Keep PRs small and focused.
- Include tests when adding features.
- Use `mvn`/`gradle` if you add build files; otherwise provide run instructions in the README.
